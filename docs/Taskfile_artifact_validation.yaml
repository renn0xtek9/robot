version: '3'

tasks:
  validate_plantuml:
    vars: 
      FILENAMES:
        sh: find -type f -name "*.plantuml"
    cmds:
      - for: {var: FILENAMES, as: FILE}
        cmd: plantuml -checkonly {{.FILE}}

  validate_document_artifacts:
    deps: [validate_plantuml]


  documentation:
    deps: [validate_document_artifacts]
    cmds:
      - mkdocs build

  serve_documentation:
    cmds:
      - mkdocs serve --clean
